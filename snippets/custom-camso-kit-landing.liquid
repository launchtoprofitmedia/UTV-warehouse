{%- comment -%}
  Per-kit landing copy lives on alternate page templates
  (camso-x4s, camso-t4s-kit, camso-4s1-kits). Assign those in the page
  admin instead of switching on page.handle in the default template.
{%- endcomment -%}
{%- liquid
  assign camso_kit_heading = ''
  assign camso_kit_body = ''
  assign camso_kit_breadcrumb = ''
  assign camso_kit_collection_handle = ''
  assign camso_kit_fitment_heading = ''
  assign camso_kit_fitment_description = ''
-%}
