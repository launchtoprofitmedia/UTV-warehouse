<style>
  .custom-features-grid {
    position: relative;
    margin-top: var(--custom-features-grid-margin-top, var(--section-vertical-spacing));
    margin-bottom: var(--custom-features-grid-margin-bottom, var(--section-vertical-spacing));
    overflow: hidden;
  }

  .custom-features-grid__bg {
    position: absolute;
    inset: 0;
    z-index: 0;
    overflow: hidden;
  }

  .custom-features-grid__bg img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    max-width: none;
    margin: 0;
    display: block;
    object-fit: var(--custom-features-grid-bg-fit, cover);
    object-position: center;
  }

  .custom-features-grid__bg-overlay {
    position: absolute;
    inset: 0;
  }

  .custom-features-grid__inner {
    position: relative;
    z-index: 1;
    padding: 40px;
  }

  .custom-features-grid__header {
    text-align: center;
    margin-bottom: 40px;
  }

  .custom-features-grid__eyebrow {
    display: block;
    font-size: 14px;
    font-style: normal;
    font-weight: 700;
    line-height: 18px;
    color: #FFFFFF;
  }

  .custom-features-grid__heading {
    font-family: Goldman;
    font-size: 32px;
    font-style: normal;
    font-weight: 700;
    line-height: 36px;
    text-transform: uppercase;
    color: #FFFFFF;
    margin-bottom: 0px;
  }

  .custom-features-grid__subheading {
    font-size: 16px;
    font-style: normal;
    font-weight: 400;
    line-height: 24px;
    color: #FFFFFF;
  }

  .custom-features-grid__cards {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
  }

  .custom-features-grid__card {
    background: #ffffff;
    border-radius: 5px;
    padding: 15px;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }

  .custom-features-grid__icon {
    width: 52px;
    height: 52px;
    margin-bottom: 12px;
    object-fit: contain;
    display: block;
  }

  .custom-features-grid__icon-placeholder {
    width: 52px;
    height: 52px;
    margin-bottom: 18px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--accent-color, #e8622a);
  }

  .custom-features-grid__icon-placeholder svg {
    width: 44px;
    height: 44px;
    fill: none;
    stroke: var(--accent-color, #e8622a);
    stroke-width: 1.5;
  }

  .custom-features-grid__card-title {
    text-align: center;
    font-size: 16px;
    font-style: normal;
    font-weight: 700;
    line-height: 24px;
    color: #000000;
    margin-bottom: 0px;
    width: 60%;
  }

  .custom-features-grid__card-text {
    color:  #5F5F5F;
    font-size: 14px;
    font-style: normal;
    font-weight: 400;
    line-height: 18px;
  }

  .custom-features-grid__card-text--richtext.rte {
    text-align: inherit;
  }

  .custom-features-grid__card-text--richtext.rte > *:first-child {
    margin-top: 0;
  }

  .custom-features-grid__card-text--richtext.rte > *:last-child {
    margin-bottom: 0;
  }

  @media screen and (max-width: 989px) {
    .custom-features-grid__cards {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media screen and (max-width: 599px) {
    .custom-features-grid__cards {
      grid-template-columns: repeat(2, 167px);
      column-gap: 10px;
      row-gap: 10px;
      justify-content: center;
    }

    .custom-features-grid__card {
      display: flex;
      box-sizing: border-box;
      width: 167px;
      height: 155px;
      padding: 15px;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      gap: 12px;
    }

    .custom-features-grid__icon {
      width: 55px;
      height: 55px;
      flex-shrink: 0;
      margin-bottom: 0;
    }

    .custom-features-grid__icon-placeholder {
      width: 55px;
      height: 55px;
      margin-bottom: 0;
      flex-shrink: 0;
    }

    .custom-features-grid__icon-placeholder svg {
      width: 46px;
      height: 46px;
    }

    .custom-features-grid__card-title {
      width: 100%;
      margin: 0;
      font-size: 14px;
      font-style: normal;
      font-weight: 700;
      line-height: 18px;
      overflow-wrap: anywhere;
    }

    .custom-features-grid__card-text {
      display: none;
    }

    .custom-features-grid__heading {
      font-size: 20px;
      line-height: 27px;
      width: 100%;
    }
    .custom-features-grid__eyebrow{
      font-size: 14px;
    }
    .custom-features-grid__subheading {
      font-size: 14px;
      line-height: 20px;
    }

    .custom-features-grid__inner .container {
      padding: 0;
    }
  }
</style>

{%- liquid
  assign bg_image = section.settings.background_image
  assign overlay_color = section.settings.overlay_color
  assign overlay_opacity = section.settings.overlay_opacity | divided_by: 100.0
  assign section_margin_top = section.settings.margin_top | default: 0
  assign section_margin_bottom = section.settings.margin_bottom | default: 0

  assign resolved_feature_cards = nil
  assign card_source = ''
  assign on_product_page = false
  if product != blank
    assign on_product_page = true
  endif

  assign product_mf_namespace = section.settings.product_feature_metafield_namespace | strip | default: 'custom'
  assign product_mf_key = section.settings.product_feature_metafield_key | strip | default: 'feature_cards'
  assign mf_namespace = section.settings.collection_feature_metafield_namespace | strip | default: 'custom'
  assign mf_key = section.settings.collection_feature_metafield_key | strip | default: 'feature_cards'
  assign try_product_feature_mf = true
  if section.settings.use_product_feature_metafield == false
    assign try_product_feature_mf = false
  endif
  assign try_collection_feature_mf = true
  if section.settings.prefer_collection_feature_metafield == false
    assign try_collection_feature_mf = false
  endif

  comment
    Product pages: product metafield list of Feature Card metaobjects first.
    Collection pages: collection metafield. Never use theme-editor blocks on a product page.
  endcomment
  if try_product_feature_mf and on_product_page
    assign pmf = product.metafields[product_mf_namespace][product_mf_key]
    if pmf != blank and pmf.value != blank
      assign product_card_count = 0
      for card in pmf.value
        assign product_card_count = forloop.length
        break
      endfor
      if product_card_count > 0
        assign resolved_feature_cards = pmf.value
        assign card_source = 'metaobjects'
      endif
    endif
  endif

  if card_source == '' and try_collection_feature_mf and on_product_page == false
    if collection != blank
      assign cmf = collection.metafields[mf_namespace][mf_key]
      if cmf != blank and cmf.value != blank
        assign resolved_feature_cards = cmf.value
        assign card_source = 'metaobjects'
      endif
    endif
  endif

  if card_source == '' and on_product_page == false and section.settings.feature_cards != blank
    assign resolved_feature_cards = section.settings.feature_cards
    assign card_source = 'metaobjects'
  endif

  if card_source == '' and on_product_page == false and section.blocks.size > 0
    assign card_source = 'blocks'
  endif

  assign product_type_label = 'product'
  if product != blank and product.type != blank
    assign product_type_label = product.type
  endif

  assign header_eyebrow = section.settings.eyebrow | replace: '[product_type]', product_type_label | replace: '{product_type}', product_type_label
  assign header_heading = section.settings.heading | replace: '[product_type]', product_type_label | replace: '{product_type}', product_type_label
  assign header_subheading = section.settings.subheading | replace: '[product_type]', product_type_label | replace: '{product_type}', product_type_label

  if on_product_page
    assign header_eyebrow = product.title | prepend: 'Why this ' | append: '?'
    assign header_heading = product.metafields[product_mf_namespace].features_heading | metafield_text | strip
    assign header_subheading = product.metafields[product_mf_namespace].features_subheading | metafield_text | strip
  endif
-%}

{%- if card_source != '' -%}
<section
  class="custom-features-grid"
  id="custom-features-grid-{{ section.id }}"
  data-section-id="{{ section.id }}"
  data-section-type="custom-features-grid"
  style="--custom-features-grid-margin-top: {{ section_margin_top }}px; --custom-features-grid-margin-bottom: {{ section_margin_bottom }}px; --custom-features-grid-bg-fit: {{ section.settings.background_image_fit | default: 'cover' }};">
  <div class="custom-features-grid__bg" aria-hidden="true">
    {%- if bg_image != blank -%}
      {{
        bg_image | image_url: width: 2000 | image_tag: loading: 'lazy', sizes: '100vw', widths: '800,1000,1400,1800,2000', alt: bg_image.alt
      }}
    {%- endif -%}
    <div class="custom-features-grid__bg-overlay" style="background-color: {{ overlay_color }}; opacity: {{ overlay_opacity }};"></div>
  </div>

  <div class="custom-features-grid__inner">
    <div class="container">

      {%- if header_eyebrow != blank or header_heading != blank or header_subheading != blank -%}
        <div class="custom-features-grid__header">
          {%- if header_eyebrow != blank -%}
            <span class="custom-features-grid__eyebrow">{{ header_eyebrow | escape }}</span>
          {%- endif -%}
          {%- if header_heading != blank -%}
            <h2 class="custom-features-grid__heading">{{ header_heading | escape }}</h2>
          {%- endif -%}
          {%- if header_subheading != blank -%}
            <p class="custom-features-grid__subheading">{{ header_subheading | escape }}</p>
          {%- endif -%}
        </div>
      {%- endif -%}

      {%- if card_source != '' -%}
        <div class="custom-features-grid__cards">
          {%- if card_source == 'metaobjects' -%}
            {%- for card in resolved_feature_cards -%}
              {%- liquid
                assign mo_icon = card.icon.value
                assign mo_title = card.title.value
                assign mo_description = card.description
              -%}
              <div class="custom-features-grid__card" data-feature-index="{{ forloop.index }}">
                {%- if mo_icon != blank -%}
                  {{
                    mo_icon
                    | image_url: width: 104
                    | image_tag: loading: 'lazy', class: 'custom-features-grid__icon', widths: '52,104', sizes: '52px', alt: mo_icon.alt
                  }}
                {%- else -%}
                  <div class="custom-features-grid__icon-placeholder">
                    <svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
                      <circle cx="12" cy="12" r="9" />
                      <path d="M12 7v5l3 3" />
                    </svg>
                  </div>
                {%- endif -%}

                {%- if mo_title != blank -%}
                  <h3 class="custom-features-grid__card-title">{{ mo_title | escape }}</h3>
                {%- endif -%}

                {%- if mo_description != blank -%}
                  {%- if mo_description.type == 'rich_text_field' -%}
                    <div class="custom-features-grid__card-text custom-features-grid__card-text--richtext rte">
                      {{ mo_description | metafield_tag }}
                    </div>
                  {%- else -%}
                    <p class="custom-features-grid__card-text">{{ mo_description.value | escape | newline_to_br }}</p>
                  {%- endif -%}
                {%- endif -%}
              </div>
            {%- endfor -%}
          {%- else -%}
            {%- for block in section.blocks -%}
              <div class="custom-features-grid__card" {{ block.shopify_attributes }}>
                {%- if block.settings.icon != blank -%}
                  {{
                    block.settings.icon | image_url: width: 104 | image_tag: loading: 'lazy', class: 'custom-features-grid__icon', widths: '52,104', sizes: '52px', alt: block.settings.icon.alt
                  }}
                {%- else -%}
                  <div class="custom-features-grid__icon-placeholder">
                    <svg
                      viewBox="0 0 24 24"
                      xmlns="http://www.w3.org/2000/svg"
                      aria-hidden="true">
                      <circle
                        cx="12"
                        cy="12"
                        r="9" />
                      <path d="M12 7v5l3 3" />
                    </svg>
                  </div>
                {%- endif -%}

                {%- if block.settings.title != blank -%}
                  <h3 class="custom-features-grid__card-title">{{ block.settings.title | escape }}</h3>
                {%- endif -%}

                {%- if block.settings.description != blank -%}
                  <p class="custom-features-grid__card-text">{{ block.settings.description }}</p>
                {%- endif -%}
              </div>
            {%- endfor -%}
          {%- endif -%}
        </div>
      {%- endif -%}

    </div>
  </div>
</section>
{%- endif -%}

{% schema %}
  {
    "name": "Custom Features Grid",
    "settings": [
      {
        "type": "header",
        "content": "Background"
      },
      {
        "type": "header",
        "content": "Spacing"
      },
      {
        "type": "range",
        "id": "margin_top",
        "label": "Margin top",
        "min": 0,
        "max": 160,
        "step": 4,
        "unit": "px",
        "default": 0
      },
      {
        "type": "range",
        "id": "margin_bottom",
        "label": "Margin bottom",
        "min": 0,
        "max": 160,
        "step": 4,
        "unit": "px",
        "default": 0
      },
      {
        "type": "image_picker",
        "id": "background_image",
        "label": "Background image"
      },
      {
        "type": "select",
        "id": "background_image_fit",
        "label": "Background image sizing",
        "default": "cover",
        "options": [
          {
            "value": "cover",
            "label": "Cover (fills area, crops edges)"
          },
          {
            "value": "contain",
            "label": "Contain (full image visible, may letterbox)"
          }
        ]
      },
      {
        "type": "color",
        "id": "overlay_color",
        "label": "Overlay color",
        "default": "#1a0a00"
      },
      {
        "type": "range",
        "id": "overlay_opacity",
        "label": "Overlay opacity",
        "min": 0,
        "max": 100,
        "step": 5,
        "unit": "%",
        "default": 0
      }, {
        "type": "header",
        "content": "Heading"
      }, {
        "type": "paragraph",
        "content": "On product pages, the eyebrow is “Why this [product title]?”. Heading and subheading show only when `custom.features_heading` and `custom.features_subheading` are set."
      }, {
        "type": "text",
        "id": "eyebrow",
        "label": "Eyebrow text",
        "default": "Why this [product_type]?",
        "info": "Used only when there is no product (collection or other templates). Product pages always show “Why this [product title]?”."
      }, {
        "type": "text",
        "id": "heading",
        "label": "Heading",
        "default": "Built for riders who ride everything",
        "info": "Used on collection and other non-product templates. On product pages this is hidden unless `custom.features_heading` is set."
      }, {
        "type": "textarea",
        "id": "subheading",
        "label": "Subheading",
        "default": "Every feature engineered for how you ride.",
        "info": "Used on collection and other non-product templates. On product pages this is hidden unless `custom.features_subheading` is set."
      },
      {
        "type": "header",
        "content": "Product feature metafield"
      },
      {
        "type": "paragraph",
        "content": "On product pages, cards come from `custom.feature_cards` (list of Feature Card metaobjects). The eyebrow is “Why this [product title]?”. Heading and subheading show only when `custom.features_heading` and `custom.features_subheading` are set. The section is hidden when the card list is empty."
      },
      {
        "type": "checkbox",
        "id": "use_product_feature_metafield",
        "label": "Use product metafield on product pages",
        "default": true,
        "info": "Settings → Custom data → Products. List of metaobject references."
      },
      {
        "type": "text",
        "id": "product_feature_metafield_namespace",
        "label": "Product metafield namespace",
        "default": "custom"
      },
      {
        "type": "text",
        "id": "product_feature_metafield_key",
        "label": "Product metafield key",
        "default": "feature_cards",
        "info": "List of metaobject refs → definition `feature_card`."
      },
      {
        "type": "header",
        "content": "Collection feature metafield"
      },
      {
        "type": "paragraph",
        "content": "On collection pages, collection metafield `custom.feature_cards` (list of metaobject refs). Not used on product pages."
      },
      {
        "type": "checkbox",
        "id": "prefer_collection_feature_metafield",
        "label": "Use collection metafield when set",
        "default": true,
        "info": "Read lists from Collections on collection templates. Disable to rely on section picker or blocks below only."
      },
      {
        "type": "text",
        "id": "collection_feature_metafield_namespace",
        "label": "Collection metafield namespace",
        "default": "custom",
        "info": "Settings → Custom data → Metafields → Collections."
      },
      {
        "type": "text",
        "id": "collection_feature_metafield_key",
        "label": "Collection metafield key",
        "default": "feature_cards",
        "info": "List of metaobject refs → definition `feature_card`."
      },
      {
        "type": "text",
        "id": "priority_collection_handle",
        "label": "Preferred collection handle (optional)",
        "info": "Unused on product pages. Product cards come from the product metafield above."
      },
      {
        "type": "header",
        "content": "Section fallback metaobjects"
      },
      {
        "type": "paragraph",
        "content": "On non-product templates, used when the collection metafield is empty. Not used on product pages. Fields: icon, title, description on type `feature_card`."
      },
      {
        "type": "metaobject_list",
        "id": "feature_cards",
        "label": "Feature cards (section)",
        "metaobject_type": "feature_card",
        "limit": 12
      }
    ],
    "blocks": [
      {
        "type": "feature_item",
        "name": "Feature item",
        "settings": [
          {
            "type": "image_picker",
            "id": "icon",
            "label": "Icon image"
          }, {
            "type": "text",
            "id": "title",
            "label": "Title",
            "default": "Certified Safe — Full Stop"
          }, {
            "type": "textarea",
            "id": "description",
            "label": "Description",
            "default": "DOT and ECE certified. Dual-density EPS liner absorbs impact progressively, keeping your head protected across a range of crash forces."
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "Custom Features Grid",
        "category": "Content",
        "blocks": [
          {
            "type": "feature_item",
            "settings": {
              "title": "Certified Safe — Full Stop",
              "description": "DOT (FMVSS 218) and ECE certified. Dual-density EPS liner absorbs impact progressively, keeping your head protected across a range of crash forces — not just the test standard minimum."
            }
          },
          {
            "type": "feature_item",
            "settings": {
              "title": "See Everything, Fog Nothing",
              "description": "Anti-fog, anti-scratch face shield with tool-less changes. Integrated drop-down sunshade means you stop fumbling with sunglasses mid-trail. Shield lock stays secure on rough terrain."
            }
          },
          {
            "type": "feature_item",
            "settings": {
              "title": "Cool When You Need It, Sealed When You Don't",
              "description": "Switchable ventilation in chin bar, brow, and rear. Dial it open for blazing summer trails, sealed tight for cold high-altitude passes. Works in both configs without removing the helmet."
            }
          },
          {
            "type": "feature_item",
            "settings": {
              "title": "Goggle-Ready for Off-Road Missions",
              "description": "Wide eyeport fits most standard MX goggles. 100% UVA/B protection up to 400nm. Side-plate covers convert it to street-legal in seconds — one helmet, two worlds."
            }
          }, {
            "type": "feature_item",
            "settings": {
              "title": "Fresh After Every Ride",
              "description": "Removable, washable SpaSoft antimicrobial liner. Moisture-wicking, odor-resistant. Take it out after a long ride, toss it in the wash. No more funky helmet smell by mid-season."
            }
          }, {
            "type": "feature_item",
            "settings": {
              "title": "Visible at Dawn & Dusk",
              "description": "3M reflective details integrated into the conceal pattern graphics. Low-profile enough to blend into the trail — visible enough for early morning and evening rides on the road."
            }
          }
        ]
      }
    ]
  }
{% endschema %}